###
# kubernetes scheduler config

# default config should be adequate

# Add your own!
KUBE_SCHEDULER_ARGS="--leader-elect=true --address=127.0.0.1 --kubeconfig=/etc/kubernetes/kube-scheduler.kubeconfig --ipam-address={{ service_wocloud_ipam_ip }}"
